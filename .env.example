# Copy to .env and adjust as needed for local development.

# Postgres/pgvector connection used by src/db.py, Alembic, and dbt.
DATABASE_URL=postgresql+psycopg://signalgraph:signalgraph@localhost:5432/signalgraph

# Optional: required only if src/ingestion/companies_house.py is invoked directly.
# Not used by the default synthetic-data demo pipeline.
COMPANIES_HOUSE_API_KEY=
